export interface Locale {
  site_title: string;
  home: {
    welcome: {
      title: string;
      subtitle: string;
    };
    form: {
      title: string;
      subtitle: string;
      message: string;
      toBoard: string;
      toAdvisors: string;
      selectAdvisors: string;
      contactEmail: string;
      contactEmailExplanation: string;
      submit: string;
      required: string;
      invalidEmail: string;
      invalid: string;
    }
  }
}