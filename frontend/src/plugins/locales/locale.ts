export interface Locale {
  site_title: string;
  home: {
    welcome: {
      title: string;
      subtitle: string;
    };
    form: {
      title: string;
      subtitle: string;
      message: string;
    }
  }
}